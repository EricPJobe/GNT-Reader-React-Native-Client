export const updateReferenceFrom = content => ({
    type: "UPDATE_REFERENCE_FROM",
    payload: {
        content
    }
});

export const updateReferenceTo = content => ({
   type: "UPDATE_REFERENCE_TO",
   payload: {
       content
   }
});

